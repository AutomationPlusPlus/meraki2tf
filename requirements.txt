# Approved dependencies only — see CLAUDE.md Dependency Escalation Policy.
# Runtime closure with hashes for unattended workers: requirements-lock.txt.
meraki>=3.0,<5
pre-commit
flake8
mypy
tox
pytest
pytest-cov
pip-audit
